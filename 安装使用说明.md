# 文案与模板矩阵视频 Skill v1.9.0

这个安装包包含两个独立功能：文案一键成片，以及默认使用的 `text-media-text` 模板成片。明确的“模板成片配音”仍属于模板成片；只有完整文案、逐镜口播、语义分镜或语音同步字幕才进入文案一键成片。模板成片现在支持两条路径：HQ CLI 0.15.4+ 调用网站当前模板、平台素材库、公共/个人复刻音色和语速；本地路径继续使用 8 套 FFmpeg 与 18 套 HyperFrames 内置模板。两条路径都禁止 AI 生成模板素材。HQ CLI 路径先报价、经用户确认后提交，并只恢复原任务；本地路径仍需先连接素材库并通过 `inspect`。完整主站合同见 `script-to-matrix-video/references/hosted-template-cli.md`。

完整功能说明和输入示例见 `功能介绍.md`；Windows 安装、环境和素材库配置见 `INSTALL.md`。

首次安装先连接本地素材库：

```powershell
python .\script-to-matrix-video\scripts\material_library.py connect --root "D:\media\your-library"
```

或连接已配置 SSH 密钥的远程素材库：

```powershell
python .\script-to-matrix-video\scripts\material_library.py connect --host YOUR_SSH_ALIAS --user YOUR_USER --remote-root /absolute/library/path
```

验证成功后安装：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1
```

覆盖旧版并保留备份：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Force
```

安装完成后重启 Codex。素材库连接保存在个人目录，不进入 Skill 分享包，也不保存密码或私钥。
