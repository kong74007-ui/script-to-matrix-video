# 文案与模板矩阵视频 Skill v1.6.0

这个安装包包含两个独立功能：文案一键成片，以及 `text-media-text` 模板成片。Skill 支持通过个人 SSH 连接配置直接读取、搜索和下载素材库内容。模板成片可以调用客户素材和素材库中状态为“可使用”的素材，但禁止 AI 生成素材；默认无配音，支持单条、多版本和批量生成，并可记录批次总耗时和每个视频的渲染耗时。模板最低 8 秒并按文案自动延长；按时长强制使用多个不同素材、默认至少包含一个视频；批量任务强制轮换 BGM。安装包还包含 12 条模板案例视频，可在 `assets/examples/text-media-text` 中查看。

完整功能说明和输入示例见 `功能介绍.md`；Windows 安装、环境和素材库配置见 `INSTALL.md`。

快速安装：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1
```

覆盖旧版并保留备份：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Force
```

安装完成后重启 Codex。
