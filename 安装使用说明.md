# 文案与模板矩阵视频 Skill v1.8.7

这个安装包包含两个独立功能：文案一键成片，以及默认使用的 `text-media-text` 模板成片。未明确指定功能时直接进入模板成片；只有明确要求完整文案、口播配音或语义分镜时才进入文案一键成片。每台新电脑必须先连接并验证自己的素材库，未通过 `inspect` 时首次安装和首次渲染都会停止。模板成片可以调用客户素材和素材库中状态为“可使用”的素材，但禁止 AI 生成素材；默认无配音，支持单条、多版本和批量生成，并可记录批次总耗时和每个视频的渲染耗时。Skill 内置 8 套标准 FFmpeg 模板和 18 套 HyperFrames 参考排版模板，共 26 套；默认使用 `template_id: black-left-bold`，并提供 26 条案例视频和首帧预览。参考排版模板需要 Node.js/npm，其他功能继续要求 Python、FFmpeg 和 FFprobe。

完整功能说明和输入示例见 `功能介绍.md`；Windows 安装、环境和素材库配置见 `INSTALL.md`。

首次安装先连接本地素材库：

```powershell
python .\script-to-matrix-video\scripts\material_library.py connect --root "D:\media\your-library"
```

或连接已配置 SSH 密钥的远程素材库：

```powershell
python .\script-to-matrix-video\scripts\material_library.py connect --host YOUR_SSH_ALIAS --user YOUR_USER --remote-root /absolute/library/path
```

验证成功后安装：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1
```

覆盖旧版并保留备份：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Force
```

安装完成后重启 Codex。素材库连接保存在个人目录，不进入 Skill 分享包，也不保存密码或私钥。
