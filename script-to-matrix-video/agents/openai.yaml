interface:
  display_name: "文案与模板矩阵视频"
  short_description: "默认生成上文中素材下文模板成片，也支持完整文案一键成片"
  default_prompt: "使用 $script-to-matrix-video 默认按 text-media-text 模板成片，生成可发布的9:16矩阵视频；只有我明确要求完整文案、配音或语义分镜时才使用文案一键成片。"
policy:
  allow_implicit_invocation: true
