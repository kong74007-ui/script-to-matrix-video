# 文案与模板矩阵视频 Skill v1.8.7

> ## ⚠️ 黄雀主站现状（2026-09-10 起，先读这段）
>
> 黄雀主站上「模板成片」已平台化：由主站矩阵模板能力（matrix-template-*，20 个模板：full-overlay-bold / poster-split + ref-01~ref-17 + nine-grid-reveal）在渲染机集群出片，成片子 Agent 用 `hq capabilities` / `hq run` 调用；本仓库的本地渲染器在主站**已退役**。
>
> **训练子 Agent 请读 `script-to-matrix-video/SKILL.md`（平台版正文）+ `references/production-platform.md`（平台对接完整规范）**。本文档下文描述的是本地渲染器时代的两个功能，仅归档参考。

2026-09-10 更新：在 `nine-grid-reveal` 九宫格开场·全屏展示基础上，新增 `triple-strip-shutter` 三横屏开场·光栅快切和 `yellow-banner-zoom` 黄条标题·变幅冲击；后两套源模板与绑定 BGM 经用户本次授权公开同步至 GitHub，精简后当前仓库模板总数为 21 套，原 8 套标准模板及其案例已移除。

这个 Skill 有两个并列、独立的成片功能，默认进入“模板成片”。两者共用素材库、BGM、渲染和质检能力，但输入方式和生产链路不同。每台新电脑首次安装时必须先连接自己的素材库并通过 `material_library.py inspect`；连接未通过前不会创建项目或渲染。AI 图片补缺只允许用于“文案一键成片”；“模板成片”禁止生成 AI 素材。

| 独立功能 | 最少需要输入 | 默认成片方式 |
| --- | --- | --- |
| 功能一：文案一键成片 | 一份完整客户文案 | 理解全文、语义分镜、逐镜匹配素材、可选配音、字幕、转场、封面，输出完整 MP4 |
| 功能二：模板成片（默认） | 上方标题＋下方副标题/CTA；也可只给主题并让 Skill 写文案 | 固定上文字、中间素材、下文字；只用客户或素材库素材，默认无配音，可单条、多版本或批量输出 MP4 |

当用户没有指定功能、只说“出个视频”、只给一个主题，或提供标题/CTA、截图、表格时，直接使用模板成片，不再询问选择功能。只有用户明确要求完整文案成片、口播配音、语义分镜或保留长文案为完整视频时，才使用功能一。

## 功能一：文案一键成片

适合知识口播、案例解析、行业观点、故事讲解和矩阵引流内容。

只需提供完整文案。Skill 会理解全文背景和目标受众，再按语义拆分分镜；每个分镜可使用一至三个图片或视频素材。素材优先级为客户素材、状态为“可使用”的素材库记录、AI 生成图片。随后完成可选阿里云 CosyVoice 配音、音频定时、字幕、音效、BGM、轻运镜、转场、拼接、首帧封面和 MP4 渲染。

默认输出 1080×1920、30 fps、H.264/AAC 通用竖屏母版。总时长由文案、分镜和实际音频决定，不强行限制在固定秒数。

示例：

```text
使用 $script-to-matrix-video 的“文案一键成片”功能，把下面客户文案制作成9:16矩阵引流视频。优先使用素材库，不足时生成AI图片；使用阿里配音，BGM自动，直接输出MP4：……
```

## 功能二：模板成片（`text-media-text`，默认）

适合行业数据、招商引流、圈层活动、项目介绍、案例标题和固定 CTA 视频。它是独立功能，不需要先提供一篇长口播稿。

画面固定为：

- 上部：标题和重点数据；
- 中部：与完整文案语义匹配的图片或视频素材；
- 下部：副标题、行动指令或评论区关键词。

离线默认使用 `ref-15-tianjin-monochrome`（天津黑白极简），不限定文案主题。8 个标准 FFmpeg 预设已删除，保留 18 个参考排版和 3 个独立动效模板。参考排版使用 `render_reference_typography.py`；每层字体、字号、描边、对齐和位置预设，文字从首帧显示，不做渐入。默认无配音，模板成片禁止 AI 生成素材。

参考模板文案映射到 `top1/top2/top3/bottom1/bottom2`；字体随 Skill 保存，不依赖本机安装。三套独立动效使用下方专属输入，保留固定时间线和绑定 BGM。

18 套 `ref-` 参考排版每条任务自动随机为 8–15 秒，并固定使用 3 段不同视频素材，用于短钩子、活动邀约与行业私域内容。三个输入视频会先在任务目录循环补足到 15 秒，再按随机时长完成成片，防止短素材或最后一段提前结束出现黑屏。选择这组模板时，长文案必须先缩短或拆条，不允许通过缩小字号破坏模板效果。

**模板成片素材规则：只允许使用客户提供的素材，或素材库中状态为“可使用”的图片和视频。禁止调用 AI 图片或视频生成模型。** 如果检索不到与完整文案语义匹配的素材，该条任务标记为 `material_missing` 并说明已经检索的方向；不得用不相关素材填充，批量中的其他任务继续执行。

保留的参考排版和三套独立动效模板均使用已审核视频，不使用纯图片兜底。

### 九宫格独立输入

指定 `nine-grid-reveal`、九宫格开场或九宫格接全屏展示时，使用 [九宫格模板流程](script-to-matrix-video/references/nine-grid-reveal.md)。画面固定为 1080×1920、30 fps、12 秒，标题和 CTA 从第 0 帧显示至结尾；九格逐个显现，第 96 帧切全屏并保留一帧放大和模糊冲击，此后恢复正常比例，在 6.3 秒和 9 秒切换后续视频。

输入为 `title`（最多 9 字符）、`tagline`（最多 16 字符）、9 条不同的 `grid` 视频和 3 条 `main` 视频。`main` 可以复用九格素材；各项必须指向客户提供或素材库已审核的本地视频。标题、CTA 和素材可以替换，时长和音乐按模板复用。

音乐固定绑定模板内 `assets/audio/reference-bgm.m4a`，本次已授权随模板同步至仓库。准备脚本校验文件完整性后原样复制，任务无需传 `bgm`，不检索新音乐、不随机换曲、不参与批量 BGM 轮换。用户明确要求换曲或静音时，作为单独定制处理。

运行 `scripts/prepare_nine_grid.py` 准备新项目，再使用 HyperFrames `0.8.33` 检查和渲染。准备时将 HLG/PQ 素材实际转换为 Rec.709 SDR，保留素材来源记录；输出检查包括首帧文字、九格顺序、全屏冲击、12 秒时长及绑定音轨。

### 三横屏开场·光栅快切独立输入

指定 `triple-strip-shutter` 时使用 [三横屏模板流程](script-to-matrix-video/references/triple-strip-shutter.md)。模板固定 1080×1920、30 fps、17.6 秒（528 帧）：三条横向视频带同时开场 3.9 秒，随后进入五段全屏素材与固定光栅快切。四层文案从第 0 帧显示至结尾，不做文字渐入。

可替换字段为 `title`、`subtitle`、`ctaLine1`、`ctaLine2`；素材输入为三条不同的 `opening` 视频和五条 `main` 视频，后者至少来自三个不同原素材、可以复用开场视频。只允许客户提供或素材库状态为“可使用”的视频，禁止 AI 素材。素材必须覆盖所需片段，不循环补时或冻结尾帧。

使用 `scripts/prepare_triple_strip.py` 准备独立任务目录，再用 HyperFrames `0.8.33` 检查与渲染。模板源与原绑定 BGM 经用户本次授权公开同步至仓库；音乐随模板复制和复用，不随机换曲、不参与批量音乐轮换。标题、CTA 和视频可替换，字体排版、17.6 秒时间线和绑定音乐保持模板设置。

### 黄条标题·变幅冲击独立输入

指定 `yellow-banner-zoom` 时使用 [黄条标题模板流程](script-to-matrix-video/references/yellow-banner-zoom.md)。模板固定 1080×1920、30 fps、302 帧（约 10.0667 秒）：首尾横向视频带搭配同素材模糊背景，中段全屏，保留变幅与径向冲击切换。

可替换字段为 `title`、`subtitle1`、`subtitle2`、`sourceLabel`、`body`、`cta`；所有非空文字从第 0 帧显示至结尾。`media` 必须包含三条不同源视频，只允许客户提供或素材库状态为“可使用”的视频，禁止 AI 素材。素材须覆盖对应的 86、97、119 帧，不循环、冻结尾帧或补黑帧。

使用 `scripts/prepare_yellow_banner.py` 准备独立任务目录，再用 HyperFrames `0.8.33` 检查与渲染。模板源与原绑定 BGM 经用户本次授权公开同步至仓库；音乐随模板复制和复用，不随机换曲、不参与批量音乐轮换。两套新模板都使用固定帧数，不采用标准模板的阅读时长公式或 `ref-*` 的随机 8–15 秒规则。以下普通单条与批量字段适用于离线参考排版；三套独立动效模板使用各自专属输入。

### 单条输入

必填：

- `top_text`：上方标题；
- `bottom_text`：下方副标题或 CTA。

可选：

- `context`：业务背景、行业、受众和内容目的；
- `variants`：同一条文案生成几个版本，默认 1；
- 素材：三个不同的已审核视频；
- 时长不输入：每条准备时随机 8–15 秒；
- `bgm`：自动、指定曲目或关闭；
- `style`：默认强视觉或克制编辑卡片。

示例：

```text
使用 $script-to-matrix-video 的“模板成片”功能，不要配音。
上方标题：大健康行业的私域复购率是40%，美妆只有15%。选赛道这件事，数据不会骗人。
下方副标题：现在还想进军这个赛道的，评论区回复：关键词
中间素材只能使用客户素材或素材库中状态为“可使用”的图片/视频，禁止AI生成；生成2个不同素材版本，BGM自动，直接输出MP4。
```

也可以只给主题，让 Skill 先生成标题和 CTA，再直接成片：

```text
使用 $script-to-matrix-video，围绕“AI 工作流”写一条观点反差型文案并直接出视频。
```

### 批量输入

可直接提供多张截图、表格、JSON 或逐条文本。每条至少包含上方标题和下方副标题/CTA，例如：

```json
[
  {
    "top_text": "第一条标题",
    "bottom_text": "评论区回复：关键词A",
    "variants": 2
  },
  {
    "top_text": "第二条标题",
    "bottom_text": "评论区回复：关键词B",
    "variants": 2
  }
]
```

从截图输入时，只提取视频文案和明确可识别的关键词，不复制播放按钮、进度条、账号标签等平台界面。遮挡字只有在含义明确时才还原，否则保留可见文字或要求补充。

批量任务会为每条文案建立独立项目，并按机器能力安全并发。相同文案的多个版本会改变素材组合、裁切或起始位置、强调色或高亮方式，不会只复制并改文件名。某一条失败不会中止其他任务。

离线参考排版批量开启 BGM 时，先从素材库选出至少 3 首合适曲目再轮换：2–3 条成片至少使用 2 首，4 条及以上至少使用 3 首；A/B 版本使用不同 BGM，相邻成片不得重复同一首。渲染前必须运行对应批量校验器，单素材、无理由纯图片、A/B 素材集合相同、BGM 未轮换或时长不足都会被阻止进入渲染。三套独立动效模板的批量任务仍各自复用绑定音乐。

批量输出包括：

- 所有成功的 MP4；
- 一个便于传输的 ZIP；
- CSV 或 JSON 耗时报告，记录批次开始/结束时间、总耗时、每个视频渲染秒数、成功/失败状态和文件路径。

示例：

```text
使用 $script-to-matrix-video 的“模板成片”批量功能，提取这些截图中的文案，每条生成2个不同素材版本；只使用客户素材或素材库中状态为“可使用”的素材，禁止AI生成；不要配音，BGM自动，同时生成并记录总时间和单条耗时。
```

## 两个功能共用的能力

- 首次安装必须用 `material_library.py connect` 连接本机或 SSH 素材库，并通过 `inspect` 验证非空索引；个人连接不会打包进 Skill，升级已有安装不要求重复建档；
- 两个功能都优先使用客户素材，其次使用素材库中状态为“可使用”的图片、视频和 BGM；只有“文案一键成片”可以在素材不足时用 AI 图片补缺，“模板成片”禁止 AI 生成素材；
- 先理解完整文案背景，再检索和筛选素材，避免只按单个关键词匹配；
- 可通过本地目录、环境变量或个人连接配置直连素材库，完成索引读取、语义搜索和素材下载；素材会复制到当前项目后再渲染，并保留素材库记录 ID 和来源路径；
- 默认自动输出可发布 MP4，并检查分辨率、编码、音轨和非零时长；
- 安装包不包含 API 密钥、服务器密码、SSH 密钥或客户视频；媒体资源包括下方公开案例成片、预览图，以及经用户授权同步的九宫格、三横屏和黄条标题绑定 BGM。

## 内置模板案例

Skill 保留 18 套参考排版的 MP4/JPG 案例。原 8 套标准预设及其案例已删除；三套独立动效保留模板源文件与绑定 BGM。

案例目录：`script-to-matrix-video/assets/examples/text-media-text/`

案例索引：`script-to-matrix-video/references/template-examples.md`

这些文件只用于查看成片效果、比较 A/B 素材变化和校准模板风格，不会作为新客户视频的素材重复使用。
